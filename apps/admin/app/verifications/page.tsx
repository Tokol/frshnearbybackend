'use client';
import { useEffect, useState } from 'react';
import { gql } from '@/lib/api';
type Item={id:string;kind:string;status:string;submittedAt:string;publicName?:string;businessId?:string;businessType?:string;city?:string;country?:string;applicant:{id:string;displayName?:string;email?:string;phone?:string;photoUrl?:string;roles:string[];verificationStatus:string}};
export default function Verifications(){const[items,setItems]=useState<Item[]>([]);const[error,setError]=useState('');const[busy,setBusy]=useState('');async function load(){try{const d=await gql<{adminVerificationQueue:Item[]}>(`query{adminVerificationQueue{id kind status submittedAt publicName businessId businessType city country applicant{id displayName email phone photoUrl roles verificationStatus}}}`);setItems(d.adminVerificationQueue)}catch(e){setError((e as Error).message)}}useEffect(()=>{load()},[]);async function decide(id:string,decision:string){const note=decision==='VERIFIED'?'Your account has been verified.':'Please review the information requested by our team.';setBusy(id);try{await gql(`mutation($input:ReviewVerificationInput!){reviewVerification(input:$input){id}}`,{input:{submissionId:id,decision,userMessage:note}});await load()}catch(e){setError((e as Error).message)}finally{setBusy('')}}return <><header className="page-head"><div><p className="eyebrow">TRUST & SAFETY</p><h1>Verification queue</h1><p>{items.length} applications waiting for review</p></div></header>{error&&<div className="alert">{error}</div>}<div className="queue">{items.length===0&&<div className="empty"><span>✓</span><h2>Queue is clear</h2><p>New seller applications will appear here.</p></div>}{items.map(item=><article className="application" key={item.id}><div className="avatar">{item.applicant.displayName?.[0]||'?'}</div><div className="application-main"><div><span className="badge submitted">{item.kind.replace('_',' ')}</span><h3>{item.publicName||item.applicant.displayName||'Unnamed applicant'}</h3><p>{item.applicant.email} · {item.applicant.phone||'No phone'}</p><p>{item.businessType||'Type not supplied'} · {[item.city,item.country].filter(Boolean).join(', ')||'Location not supplied'}{item.businessId?` · ID ${item.businessId}`:''}</p><small>Submitted {new Date(item.submittedAt).toLocaleString()}</small></div><div className="actions"><button disabled={busy===item.id} className="approve" onClick={()=>decide(item.id,'VERIFIED')}>Approve</button><button disabled={busy===item.id} onClick={()=>decide(item.id,'NEEDS_CHANGES')}>Request changes</button><button disabled={busy===item.id} className="reject" onClick={()=>decide(item.id,'REJECTED')}>Reject</button></div></div></article>)}</div></>}
